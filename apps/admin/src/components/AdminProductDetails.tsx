import { ChangeEvent, useState } from 'react'
import { ProductType } from '~types'

type AdminProductDetailsProps = {
  product: ProductType
}

export const AdminProductDetails = ({ product }: AdminProductDetailsProps) => {
  return "AdminProductDetails"
}
